# Backend API URL (required in production when frontend and backend are on different domains)
# Leave empty for local development (Vite proxy handles routing)
VITE_API_URL=https://api.your-domain.com
